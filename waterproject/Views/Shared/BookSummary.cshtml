@{

}

@model Book
<div class="card card-outline-primary m-1 p-1">
    <div class="bg-success p-2 text-white bg-opacity-75 p-1">
        <h3>
            @Model.Title
        </h3>
    </div>
    <br />
    <ul>
        <li>Book Author: @Model.Author</li>
        <li>Book Publisher: @Model.Publisher</li>
        <li>Book ISBN: @Model.Isbn</li>
        <li>Book Classification: @Model.Classification</li>
        <li>Book Category: @Model.Category</li>
        <li>Book Page Count: @Model.PageCount pages</li>
        <li>Book Price: $@Model.Price</li>
    </ul>

    <form method="post" asp-page="/Buy">

        <input type="hidden" asp-for="@Model.BookId" />
        <input type="hidden" name="returnUrl" value="@ViewContext.HttpContext.Request.PathAndQuery()" />

        <span class="card-text">
            <button type="submit" class="btn btn-success btn-sm float-end">Purchase</button>
        </span>
    </form>
</div>
<br />


